# Data / secrets
/data/
.env

# Claude Code
.claude/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.pytest_cache/

# PyInstaller
build/
dist/
backend/static/build/
backend/static/dist/
*.spec

# macOS
.DS_Store

.superpowers/

# Update orchestrator run state (written into the repo mount by the sidecar)
.update-state.json

# Backups of .env — admins make these; they must not block the updater's
# clean-tree preflight check.
.env.bak*
.env.*.bak
.env.backup*
